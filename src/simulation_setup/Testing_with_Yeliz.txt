CLM_FORCE_COLDSTART=on

1997-01-01

2016-12-31

check lnd.log for radiation BalanceCheck error/warnings

Consider proper spinup, see e.g.
https://escomp.github.io/ctsm-docs/versions/master/html/users_guide/running-special-cases/Spinning-up-the-biogeochemistry-BGC-spinup.html

warming data original method (applies warming to the whole year, not just summer months):

1. loop through all f files in the current directory, 
2. make a copy of the original file (add "copy_" in front of old name)
3. subset the TBOT variable and store it as new files starting with "TBOT_" with cdo selname
4. subset daytime timestamps from the new TBOT files, saved as new files starting with "TBOT_seltime", using cdo seltime
5. add 1 degree kelvin to the new subsetted files using cdo addc (= add constant), saving new files starting with "TBOT_seltime_1deg"
6. define that when merging files, values with the same time stamps should be skipped for the first file provided in the command (export SKIP_SAME_TIME=1)
7. merge the modified TBOT- and time-subsetted files ("TBOT_seltime_1deg") back into the TBOT file
8. merge the new merged TBOT variable back into the original file, and name it the original normal file name.

```
%%bash
pwd
cd ../data/ALP4_cosmorea_warmed/datmdata
pwd
for f in clm1pt_ALP4_*; do cp $f copy_$f; cdo selname,TBOT copy_$f TBOT_$f; cdo seltime,07:00:00,10:00:00,13:00:00,16:00:00 TBOT_$f TBOT_seltime_$f; cdo addc,1 TBOT_seltime_$f TBOT_seltime_1deg_$f; export SKIP_SAME_TIME=1; cdo mergetime TBOT_seltime_1deg_$f TBOT_$f TBOT_merged_$f; cdo replace copy_$f TBOT_merged_$f $f; done
```

If needed, copy this into a code cell to view the entire process using ncdump, cdo showtime, etc. in between steps.
```
%%bash
pwd
cd ../data/ALP4_cosmorea_warmed/datmdata/test
pwd
for f in clm1pt_ALP4_*; do cp $f copy_$f; cdo selname,TBOT copy_$f TBOT_$f; ncdump -h TBOT_$f; cdo showtime TBOT_$f; cdo seltime,07:00:00,10:00:00,13:00:00,16:00:00 TBOT_$f TBOT_seltime_$f; cdo showtime TBOT_seltime_$f; cdo addc,1 TBOT_seltime_$f TBOT_seltime_1deg_$f; cdo showdate TBOT_seltime_1deg_$f; export SKIP_SAME_TIME=1; cdo mergetime TBOT_seltime_1deg_$f TBOT_$f TBOT_merged_$f; cdo showtime TBOT_merged_$f; cdo replace copy_$f TBOT_merged_$f $f; done
```
